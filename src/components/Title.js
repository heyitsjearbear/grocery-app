import "./Title.css";
const Title = () => {

    return(
        <div className = "title">
            <h2>Grocery List</h2>
            <button>Add</button>
        </div>
    );
};

export default Title;