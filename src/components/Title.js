import "./Title.css";
import Card from "./UI/Card"
const Title = () => {

    return(
        <div className = "title">
            <h2>Grocery List</h2>
            <Card>Add</Card>
        </div>
    );
};

export default Title;